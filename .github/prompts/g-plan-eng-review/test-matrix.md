# 步骤 3：完整测试矩阵生成 — 详细流程

> 本文件是 `g-plan-eng-review.prompt.md` 步骤 3 的完整执行指南。

代码覆盖率不够，必须覆盖用户交互路径。

**第一步：检测测试框架**
- 先读取 `.github/copilot-instructions.md` 的 `## Testing` 节——如有，视为权威来源。
- 如无，自动检测（`package.json` → jest/vitest/playwright、`Gemfile` → rspec、`pyproject.toml` → pytest 等）。
- 如果未检测到框架，仍产出覆盖率图，但跳过测试代码生成。

**第二步：追踪每条代码路径**

读取计划文档。对每个新增的功能/服务/端点/组件，追踪数据如何流经代码——从输入来源（请求参数、props、数据库、API 调用）→ 转换（验证、映射、计算）→ 输出（数据库写入、API 响应、副作用）→ 每步可能出错的情况（null/undefined、无效输入、网络故障、空集合）。

为每个变更文件绘制 ASCII 图，显示所有函数/方法、条件分支（if/else/switch/三元/guard/提前返回）、错误路径（try/catch/错误边界）以及对其他函数的调用。

**第三步：覆盖用户流和交互边缘情况**

代码覆盖率之外，还要覆盖真实用户如何与变更代码交互：
- **用户流**：触达此代码的完整用户操作序列
- **交互边缘情况**：双击/快速重提交、导航离开（后退键、关闭标签）、提交过期数据（页面打开30分钟）、慢连接、并发操作（两个标签页、同一表单）
- **用户可见的错误状态**：每个错误处理的UI体验是什么？用户能恢复还是被卡住？
- **空/零/边界状态**：零条结果时显示什么？一万条结果？

**第四步：E2E 测试决策矩阵**

对每个分支，判断是单元测试还是 E2E 测试：

| 标注 | 规则 |
|------|------|
| `[→E2E]` | 跨3+组件/服务的常见用户流；模拟会掩盖真实故障的集成点；认证/支付/数据销毁流程 |
| `[→EVAL]` | 关键 LLM 调用；提示词模板或工具定义变更 |
| 单元测试 | 纯函数、无副作用的内部工具函数、单一函数的边缘情况 |

**回归规则（铁律）**：当覆盖率审计发现**回归**（diff 破坏了已有行为），必须将回归测试添加到计划中，作为关键要求。无需提问，不可跳过。

**第五步：输出 ASCII 覆盖率路径图**

同时包含代码路径和用户流，标注E2E标记：

```
代码路径覆盖率
===========================
[+] src/services/example.ts
    │
    ├── processData()
    │   ├── [★★★ 已测试] 主路径 + 错误输入 — example.test.ts:42
    │   ├── [GAP]         网络超时 — 无测试
    │   └── [GAP]         无效货币 — 无测试
    └── handleEdge()
        └── [★★  已测试] 基本情况 — example.test.ts:89

用户流覆盖率
===========================
[+] 核心提交流
    ├── [★★★ 已测试] 完整流程 — example.e2e.ts:15
    ├── [GAP] [→E2E]  双击提交 — 需要E2E
    └── [GAP]         导航离开 — 单元测试足够

─────────────────────────────────
覆盖率: 3/7 路径已测试 (43%)
  代码路径: 2/4 (50%)
  用户流: 1/3 (33%)
质量: ★★★: 2  ★★: 1  ★: 0
GAP: 4条路径需要测试 (1条需E2E)
─────────────────────────────────
```

**第六步：将缺失的测试添加到计划**

对图中每个 GAP，添加到计划中：指定测试文件、具体断言（输入→预期输出/行为）、类型（单元/E2E/eval）。回归测试标注为 **CRITICAL**。

**停止。** 对本节发现的每个问题，分别提问。全部解决后再进入步骤 4。

**第七步：输出测试计划制品**

将步骤 3 的覆盖率图、GAP 清单和 E2E 标记整理为独立文件 `.context/test-plan.md`，供 `/qa` 和 `/qa-only` 直接消费：

```markdown
# 测试计划 (Test Plan)
来源：.context/eng-plan.md
生成日期：YYYY-MM-DD

## 受影响页面/路由
- [列出被本次变更影响的所有页面或 API 路由]

## 关键交互流
- [从用户视角描述需要端到端验证的操作序列]

## 边缘情况矩阵
| # | 场景 | 类型 | 预期行为 | 覆盖状态 |
|---|------|------|----------|----------|

## 关键路径（必须通过）
- [列出阻塞发布的核心路径，每条附验证命令]
```
